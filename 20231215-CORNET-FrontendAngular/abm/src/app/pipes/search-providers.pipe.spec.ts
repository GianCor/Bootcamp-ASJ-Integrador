import { SearchProvidersPipe } from './search-providers.pipe';

describe('SearchProvidersPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchProvidersPipe();
    expect(pipe).toBeTruthy();
  });
});
